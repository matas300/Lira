// src/client/components/bottom-nav.ts
//
// Bottom-nav mobile — TO BE DELETED in nav-shell slice.
// NAV_ITEMS removed from sidebar (replaced by NAV_SECTIONS in nav.ts).

export function renderBottomNav(_activeRoute = ''): string {
  return `<nav class="bottom-nav"></nav>`;
}
